---
const currentYear = new Date().getFullYear();
const { currentLocale, getRelativeLocaleUrl } = Astro.props;
---

<footer
  class="rounded-lg shadow m-4 dark:bg-black/20 backdrop-blur-lg w-full xl:w-[1120px] mx-auto mb-8"
>
  <div
    class="w-full mx-auto max-w-screen-xl p-4 md:flex md:items-center md:justify-between"
  >
    <span class="text-sm sm:text-center text-yellow-200/80"
      >© {currentYear}
      <a
        href="https://github.com/FCantora/porfolio-FCantora.git"
        target="_blank"
        class="hover:underline">FCantora™</a
      > - {currentLocale === 'es' ? 'Disponible en Github.' : 'Available on Github.'}
    </span>
    <ul
      class="flex flex-wrap items-center mt-3 text-sm font-medium text-white/90 sm:mt-0"
    >
      <li>
        <a
          href={getRelativeLocaleUrl(currentLocale, "/#about")}
          class="hover:underline me-4 md:me-6">{currentLocale === 'es' ? 'Sobre mí' : 'About'}</a
        >
      </li>
      <li>
        <a
          href={getRelativeLocaleUrl(currentLocale, "/#")}
          class="hover:underline">{currentLocale === 'es' ? 'Contacto' : 'Contact'}</a
        >
      </li>
    </ul>
  </div>
</footer>
